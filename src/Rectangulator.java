public class Rectangulator{

	public static void main(String[] args){
		int length = parse.Integer(args[0]);
		int width = parse.Integer(args[1]);

		Rectangle myObj = new Rectangle(leength,width);
		system.out.println("Area of the REctangule = %d, myObj.getArea())
	}

}
